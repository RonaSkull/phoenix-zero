services:
  - type: web
    name: phoenix-zero-web
    runtime: node
    plan: free
    buildCommand: npm ci --include=dev && npm --prefix ./apps/web ci --include=dev && npx tsc -p ./libs/phoenix-zero/tsconfig.build.json && npm --prefix ./apps/web run build
    startCommand: npm --prefix ./apps/web run start -- -p $PORT
    healthCheckPath: /api/health
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: NODE_ENV
        value: production
      - key: PHOENIX_ZERO_PUBLIC_BASE_URL
        value: https://phoenix-zero-web.onrender.com
      - key: DATABASE_URL
        sync: false
      - key: PGSSLMODE
        value: require
      - key: PHOENIX_ZERO_TMP_DIR
        value: /tmp/phoenix-zero/tmp
      - key: PHOENIX_ZERO_ADMIN_TOKEN
        generateValue: true
      - key: PAYMENTS_PIX_PROVIDER
        value: asaas
      - key: ASAAS_ENV
        value: sandbox
      - key: ASAAS_API_KEY
        sync: false
      - key: ASAAS_WEBHOOK_SECRET
        generateValue: true
      - key: PAYMENTS_CRYPTO_PROVIDER
        value: nowpayments
      - key: NOWPAYMENTS_API_KEY
        sync: false
      - key: NOWPAYMENTS_IPN_SECRET
        sync: false
      - key: TWILIO_ACCOUNT_SID
        sync: false
      - key: TWILIO_AUTH_TOKEN
        sync: false
      - key: TWILIO_WHATSAPP_FROM
        sync: false
